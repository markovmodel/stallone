
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>compmolbio</groupId>
	<artifactId>Stallone</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>Stallone</name>
	<url>http://compmolbio.biocomputing-berlin.de/index.php</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- introduce a property buildNumber -->
		<maven.build.timestamp.format>yyyyMMdd</maven.build.timestamp.format>
		<buildNumber>${maven.build.timestamp}</buildNumber>
	</properties>

	<build>
		<!-- This is EVIL since it breaks the standard maven directory layout, 
			but for now it is okay -->
		<sourceDirectory>src/</sourceDirectory>
		<testSourceDirectory>test/</testSourceDirectory>
		<pluginManagement>
			<plugins>
				<plugin>
					<artifactId>maven-compiler-plugin</artifactId>
					<!-- <version>2.0.2</version> -->
					<configuration>
						<source>1.7</source>
						<target>1.7</target>
					</configuration>
				</plugin>

				<plugin>
					<groupId>com.googlecode.jace</groupId>
					<artifactId>jace-maven-plugin</artifactId>
					<version>1.2.16</version>
					<executions>
						<execution>
							<id>enhance-classes</id>
							<goals>
								<goal>enhance-java</goal>
							</goals>
							<configuration>
								<inputFile>
									<!-- The path of the input class file -->
									<!-- ${project.build.outputDirectory}/stallone/emma_xxx/ClusterCmd.class -->
									/home/marscher/workspace/stallone/target/classes/stallone/xxx_emma/ClusterCmd.class
								</inputFile>

								<outputFile>
									<!-- ${project.build.outputDirectory}/stallone/emma_xxx/ClusterCmd_e.class -->
									/home/marscher/workspace/stallone/target/classes/stallone/xxx_emma/ClusterCmd_out.class
								</outputFile>

								<deallocationMethod>
									<!-- The Java method used to deallocate the Java peer -->
									dispose
								</deallocationMethod>

								<!-- Indicates if Java peers should output library names before loading 
									them -->
								<verbose>false</verbose>

								<!-- The native libraries to load before initializing the Java peer -->
								<libraries>
									<library>first</library>
									<library>second</library>
								</libraries>
							</configuration>
						</execution>

						<execution>
							<id>generate-peer</id>
							<goals>
								<goal>generate-cpp-peer</goal>
							</goals>
							<configuration>
								<!-- The Java class to generate a peer for -->
								<classFile>
									${project.build.outputDirectory}/stallone/emma_xxx/ClusterCmd.class
								</classFile>
								<!-- The directory of the output header files -->
								<outputHeaders>${project.build.directory}/include</outputHeaders>

								<!-- The directory of the output source files -->
								<outputSources>${project.build.directory}/include</outputSources>

								<!-- Indicates if {peer_class_name}_user.h should be generated in 
									order for allow users to declare additional peer methods -->
								<userDefinedMembers>false</userDefinedMembers>
							</configuration>
						</execution>

						<execution>
							<!-- generate c++ proxies for given java classes -->
							<id>generate-proxies</id>
							<goals>
								<goal>generate-cpp-proxies</goal>
							</goals>
							<configuration>
								<!-- Directories containing the input header files. When are they 
									created?! -->
								<inputHeaders>
									<path>${project.build.sourceDirectory}/include</path>
								</inputHeaders>

								<!-- Directories containing the input source files -->
								<inputSources>
									<path>${project.build.sourceDirectory}/input_src/</path>
									<!-- path>${project.build.sourceDirectory}</path -->
								</inputSources>

								<outputHeaders>
									<!-- The directory of the output header files -->
									<path>/tmp/</path>
								</outputHeaders>

								<outputSources>
									<!-- The directory of the output source files -->
									<path>/tmp/</path>
								</outputSources>

								<!-- The path to search for Java classes -->
								<classpath>
									<path>${java.home}/lib/rt.jar</path>
								</classpath>

								<!-- The method accessibility to expose. Acceptable values include: 
									PUBLIC, PROTECTED, PACKAGE or PRIVATE. -->
								<accessibility>
									PUBLIC
								</accessibility>

								<!-- Indicates if the proxy symbols should be exported (for generating 
									DLLs/SOs) -->
								<exportSymbols>true</exportSymbols>

								<!-- Indicates whether classes should be exported even if they are 
									not referenced by the input files -->
								<minimizeDependencies>true</minimizeDependencies>
							</configuration>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<!-- profile -P buildDistribution to construct the final compressed jar 
		containing all dependencies -->
	<profiles>
		<profile>
			<id>buildDistribution</id>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-assembly-plugin</artifactId>
						<!-- Goal assembly:single is bound to maven execution phase "prepare-package" -->
						<executions>
							<!-- is executed first -->
							<execution>
								<id>distro-assembly</id>
								<phase>prepare-package</phase>
								<goals>
									<goal>single</goal>
								</goals>
								<!-- HERE, the configuration (which is loaded from file assemblyDescriptor.xml) 
									of the assembly is setup -->
								<configuration>
									<descriptorRefs>
										<descriptorRef>jar-with-dependencies</descriptorRef>
									</descriptorRefs>
								</configuration>
							</execution>

							<!-- is executed third (!) -->
							<execution>
								<id>build-final-zip</id>
								<phase>package</phase>
								<goals>
									<goal>single</goal>
								</goals>

								<configuration>
									<!-- HERE, the configuration for zip creation is set -->
									<descriptors>
										<descriptor>zipAssembly.xml</descriptor>
									</descriptors>
									<outputDirectory>distribution</outputDirectory>
									<finalName>${project.build.finalName}_${buildNumber}</finalName>
									<appendAssemblyId>false</appendAssemblyId>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<plugin>
						<groupId>com.pyx4me</groupId>
						<artifactId>proguard-maven-plugin</artifactId>
						<version>2.0.4</version>

						<!-- New version of proguard-maven-plugin from github -->
						<!-- <groupId>com.github.wvengen</groupId> <artifactId>proguard-maven-plugin</artifactId> 
							<version>2.0.5</version> -->

						<executions>
							<!-- is executed second -->
							<execution>
								<phase>prepare-package</phase>
								<goals>
									<goal>proguard</goal>
								</goals>
							</execution>
						</executions>

						<configuration>
							<injar>${project.build.finalName}-jar-with-dependencies.jar</injar>
							<outjar>${project.build.finalName}-small.jar</outjar>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<libs>
								<lib>${java.home}/lib/rt.jar</lib>
								<lib>${java.home}/lib/jsse.jar</lib>
							</libs>
							<addMavenDescriptor>false</addMavenDescriptor>

							<!-- do not include any libraries. we depend on: we are already working 
								on a jar which contains all dependencies in one jar See: previous package 
								step -->
							<includeDependency>false</includeDependency>

							<obfuscate>false</obfuscate>

							<options>
								<option>-allowaccessmodification</option>
								<option>-keep class * implements AbstractCmd</option>
								<option>-dontoptimize</option>
								<option>-keepclasseswithmembers public class * { public static
									void main(java.lang.String[]); }</option>
							</options>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>




	<dependencies>

		<!-- java netlib -->
		<dependency>
			<groupId>com.googlecode.netlib-java</groupId>
			<artifactId>netlib-java</artifactId>
			<version>0.9.3</version>
		</dependency>

		<!-- matrix-toolkits-java -->
		<dependency>
			<groupId>com.googlecode.matrix-toolkits-java</groupId>
			<artifactId>mtj</artifactId>
			<version>0.9.14</version>
		</dependency>

		<!-- jep library, stored in project path -->
		<dependency>
			<groupId>jep</groupId>
			<artifactId>jep</artifactId>
			<version>2.4.1</version>
			<scope>system</scope>
			<systemPath>${basedir}/libraries/jep-2.4.1.jar</systemPath>
		</dependency>

		<!-- junit testing -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.8.1</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>
